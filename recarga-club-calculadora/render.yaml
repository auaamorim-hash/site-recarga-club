services:
  - type: web
    name: site-recarga-club
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: VIDEO_LIBRARY_DIR
        value: /data/videos
